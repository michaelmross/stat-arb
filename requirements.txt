# Pinned to the environment the archived results were computed on
# (2026-08-21/22). Loosen the pins only after re-running the full
# validation suite -- the pandas 3.0 boolean-index change already broke
# one code path silently once.
numpy>=2.0
scipy>=1.14
pandas>=2.2
statsmodels>=0.14.6
matplotlib>=3.9

# fetch_data.py (Tiingo, token required)
requests>=2.32

# fetch_futures.py (Yahoo, no key)
yfinance>=0.2.40
